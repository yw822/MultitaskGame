var initializator = (function() {
    var initializator = {};
    Object.defineProperty(initializator, 'initializeGame1', {
        value: function () {
            //TODO: complete
        }
    });
    return initializator;
}());